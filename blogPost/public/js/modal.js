$(document).ready(function(){
    $('#btns').on('click',function(){
        console.log($('#comment').text());    
        // var target = $(this).data('target');
        // var id=$(this).data("id");
        // var comment=$(this).data("comment");
        // $(target+' #commentId').val(id);
        // $(target+' #txtArea').val(comment);
    });
  });